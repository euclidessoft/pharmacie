<form method="post" action="{{ path('app_fournisseur_delete', {'id': fournisseur.id}) }}" onsubmit="return confirm('{{ 'Voulez-vous supprimer le fournisseur?'|trans }}');"  style="display: inline;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fournisseur.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash"></i></button>
</form>
