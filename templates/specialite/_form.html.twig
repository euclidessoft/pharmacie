{{ form_start(form, {'attr' : { 'id' : 'form'}}) }}

<div class="col-lg-8 mt-4 fond pb-4 pt-3 px-5">
    <h4 class="mb-3 p-2">
        <i class="fa-brands fa-product-hunt mr-2"></i>
        {{ 'Nouvelle spécialité'|trans }}
    </h4>
    <hr class=" mb-5">
    <div class="row">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
        <div class="col-md-4 pt-3 fond-btn">
            <div class="text-center">
                {#                <div class="row">#}
                <button type="button" class="btn btn-shadow btn-success btn-normal mb-3 mr-3" id="send"
                        onclick="load_btn('send')">
                    <i class="fa fa-save"></i>
                    {{ 'Enregistrer'|trans }}
                </button>

                <button type="reset" class="btn btn-shadow btn-danger btn-normal mb-3">
                    <i class="fa fa-delete-left"></i>
                    {{ 'Effacer'|trans }}
                </button>
            </div>
            {{ form_end(form) }}
        </div>
    </div>