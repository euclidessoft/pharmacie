<!DOCTYPE html>

<title>Mutuelles</title>

{% block body %}
    <h1>Mutuelles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mutuelle.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ mutuelle.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ mutuelle.adresse }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ mutuelle.telephone }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ mutuelle.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mutuelles_index') }}">back to list</a>

    <a href="{{ path('app_mutuelles_edit', {'id': mutuelle.id}) }}">edit</a>

    {{ include('mutuelles/_delete_form.html.twig') }}
{% endblock %}
