<!DOCTYPE html>

<title>New Mutuelles</title>

{% block body %}
    <h1>Create new Mutuelles</h1>

    {{ include('mutuelles/_form.html.twig') }}

    <a href="{{ path('app_mutuelles_index') }}">back to list</a>
{% endblock %}
